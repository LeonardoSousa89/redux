import { Component } from "react";

import './card.css'

export default class Card extends Component{
    render(){
        return(
            <main className="app-card">
                card component
            </main>
        )
    }
}